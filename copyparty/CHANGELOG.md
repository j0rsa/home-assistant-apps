# Changelog

## 1.20.18

- Initial release of Copyparty file server
- Share `/share`, `/media`, `/config` (addon config), `/homeassistant, /addon-configs`, `/backup`, `/ssl` (read-only)
- Options for admin account, anonymous access mode, indexing, and server name

